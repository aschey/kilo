[package]
edition = "2021"
name = "kilo"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.141"
nix = {version = "0.26.2", default-features = false, features = ["ioctl", "term"]}
ropey = "1.6.0"
